
2026-04-02 — Task 1: Reframed root `docs/CONTEXT.md` as framework-only context and explicitly moved active-project execution ownership to `projects/<PROJECT>/docs/*`; documented `plan.json` as the execution source of truth so later command work can build on a stable ownership model.

2026-04-02 — Task 2: Rewrote root `docs/PLAN.md` as framework planning guidance only; it now states that active-project execution state lives in `projects/<PROJECT>/docs/plan.json`, that project `PLAN.md` is a generated/derived view, and that tool/document precedence remains `plan.json` -> `PLAN.md` -> `IMPLEMENTATION_PLAN.md` to avoid drift.

2026-04-02 — Task 3: Reworked root `docs/TODO.md` into a framework backlog/execution-guidance document only; clarified that active project TODOs belong under `projects/<PROJECT>/docs/TODO.md`, while structured runtime records such as `[STUCK]` remain valid in project TODO space instead of the root framework doc.

2026-04-02 — Task 4: Defined a shared command/backend contract for `INIT`, `REDEFINE`, `UPDATE_TODO`, `START_WORK`, and `FIX` in `docs/CONTEXT.md`; standardized the reusable `{status,message,data}` envelope, preserved `plan.json` ownership, clarified confirmation/degradation rules, and narrowed `/DEV_SDD:start-work` docs to user-facing workflow semantics while leaving concrete probing behavior to `start-work/run.py`.

2026-04-03 — Task 5: Aligned `projects/_template/CLAUDE.md`, `projects/_template/docs/PLAN.md`, and `/project:new` wording around project-scoped ownership: `projects/<PROJECT>/docs/plan.json` is the execution authority, project `PLAN.md` is the generated read-only view, and project `TODO.md` stays a supplemental notes/audit surface instead of the state source.

2026-04-03 — Task 6: Added a dedicated INIT helper at `.claude/tools/init/run.py` that derives bootstrap docs only from the target project’s `docs/CONTEXT.md`, generates `docs/plan.json` as the authoritative execution source, and returns structured overwrite-confirmation metadata with diff previews and a confirmation token instead of silently replacing existing project docs.

2026-04-03 — Task 7: Added `.claude/tools/redefine/run.py` and `/DEV_SDD:redefine` spec with a single propagation path rooted in project `docs/CONTEXT.md` -> `docs/plan.json` -> derived `docs/PLAN.md`/`docs/TODO.md`; retained module state for unchanged modules, emitted `{status,message,data}` plus explicit change metadata, and handled legacy `REDEFIND` only as a compatibility alias mapped to the same backend semantics.

2026-04-03 — Task 8: Defined `DEV_SDD:UPDATE_TODO` stable-ID merge flow with a managed-zone + user-notes-zone ownership split: helper `.claude/tools/update-todo/run.py` now injects missing immutable task IDs into `docs/plan.json`, uses IDs as the only merge key (supports `--ids` partial updates), preserves the user-notes zone byte-for-byte, and returns confirmation metadata (`required/token/conflicts[]`) for local managed edits or unsafe full-regenerate scenarios instead of silent overwrite.

2026-04-03 — Task 9: Enhanced `.claude/tools/start-work/run.py` with deterministic TODO reconciliation against `docs/plan.json` stable IDs (managed block only), exposing `data.reconciliation` (status/matched_ids/orphan_ids/conflict_ids/missing_ids/warnings) and `data.next_action_source`; `next_action` remains plan-driven (or handoff/session when explicitly resumed), while TODO mismatches only emit warnings/guidance and never override plan order/state.

2026-04-03 — Task 10: Added a dedicated FIX helper at `.claude/tools/fix/run.py` plus `/DEV_SDD:fix` spec; the flow now resolves the target project with the same root/project heuristics as `start-work`, reads project memory before optioning, emits a triage summary with confidence/repro/impact/memory signals, and always returns exactly two option families (`minimal_change`, `comprehensive_change`) while degrading sparse reports to low-confidence evidence-gathering advice instead of hallucinated patches.

2026-04-03 — Task 11: Unified INIT / REDEFINE / UPDATE_TODO / START_WORK / FIX on a shared resolver in `.claude/tools/workflow_cli_common.py` so framework-root detection and repo-relative project/path resolution follow one rule set; `start-work` now accepts repo-relative project paths without producing `projects/projects/...`, and `skill-tests/cases/test_workflow_cli_contracts.py` locks the shared `{status,message,data}` envelope plus cwd-stable resolution behavior across the helper family.

2026-04-03 — Final Wave follow-up: hardened `.claude/tools/start-work/run.py` for absolute external project paths by replacing direct `relative_to(ROOT)` calls with safe relative-or-absolute formatting for `project_path`, session file paths, and project context-file reporting; added a regression in `skill-tests/cases/test_start_work_tool.py` to ensure external absolute project inputs return the normal JSON envelope instead of crashing.

2026-04-03 — Task 12: Added `skill-tests/cases/test_workflow_drift_guards.py` as a deterministic Layer 1 drift suite that hard-fails when root docs reintroduce unresolved placeholders, when root `docs/PLAN.md` weakens `plan.json` authority/precedence semantics, when `UPDATE_TODO` conflict writes bypass confirmation (including wrong-token path), or when workflow shared-contract coverage drifts out of `run_all.py` registration and key assertions.
