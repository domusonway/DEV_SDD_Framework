# 项目上下文（CONTEXT.md）
# 项目: <YOUR_PROJECT_NAME>

> ⚠️ 所有 Agent 在读任何 SPEC.md 之前必须先完整阅读本文件
> 由 @planner 在分析参考项目后填写，人工审核后视为锁定
> 修改时注明日期和原因

---

## 1. 领域知识前提

> 缺乏这些知识会导致"代码能跑但结果静默偏差"的最危险问题

### 1.1 核心概念
| 概念 | 在本项目中的含义 | 常见误解（Agent 容易犯的错）|
|------|----------------|--------------------------|
| _(由 @planner 填写)_ | | |

### 1.2 数据格式约定
```
# 核心数据结构：
# 数值精度要求（dtype）：
# 坐标系/值域约定：
# 时间戳格式：
```

### 1.3 核心数据流
```
输入 → [模块A] → 中间数据 → [模块B] → ... → 输出
# 描述数据在各模块间的变换，特别是格式变化
```

---

## 2. 运行环境约束

> 说明"为什么选这个版本"，防止 Agent 随意升级依赖

### 2.1 运行环境命令
```bash
# 激活项目运行环境（填写实际命令）：
# <your_env_activation_command>
```

### 2.2 关键依赖版本
| 依赖 | 版本 | 必须此版本的原因 | 升级风险 |
|------|------|---------------|---------|
| _(由 @planner 填写)_ | | | |

### 2.3 已知版本 Bug / 不兼容点
```
# _(随开发过程补充)_
```

---

## 3. 反模式清单（⚠️ 禁止实现方式）

> 以下做法在本项目中已验证会导致问题，由 Bug 修复经验持续积累
> 新增时注明来源（哪个 Bug / 哪次调试）

| 禁止做法 | 原因 / 来源 | 正确替代 |
|---------|-----------|---------|
| _(初始为空，随 Bug 修复积累)_ | | |

---

## 4. 参考项目关键设计决策

> 理解"为什么这样设计"，而不只是"长什么样子"
> 这些理解指导实现方向，但不能直接复制代码

| 设计决策 | 设计原因 | 对实现的约束 |
|---------|---------|------------|
| _(由 @planner 分析后填写)_ | | |

---

## 5. 模块间隐式契约

> 接口定义里看不到、但实际存在的模块间约定
> 违反会导致模块单独测试通过，集成后出错

| 契约 | 方向 | 说明 |
|------|------|------|
| _(由 @planner 分析后填写)_ | M_A → M_B | |

---

## 变更历史
| 日期 | 修改内容 | 修改者 |
|------|---------|--------|
| _(创建日期)_ | 初始创建 | @planner |
