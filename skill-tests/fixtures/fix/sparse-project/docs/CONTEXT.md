# sparse-project · 项目背景与架构

---

## 项目目标
验证 FIX 在缺少复现信息时安全降级，优先请求补充上下文而不是直接猜测补丁。

---

## 背景
项目偶发报告双目同步异常，但目前只有口头反馈，没有稳定复现步骤或日志。

---

## 模块划分

### sync_pipeline
- 职责：管理左右帧配对和时序对齐
- 输入：左右相机帧
- 输出：对齐后的帧对
- 依赖：无

### export_session
- 职责：导出调试会话和异常样本
- 输入：同步结果、日志片段
- 输出：诊断包
- 依赖：sync_pipeline
